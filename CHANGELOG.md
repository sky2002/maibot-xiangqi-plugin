# 更新日志

## 0.5.0

### 主要功能

- bot 直接采用引擎原生 bestmove，取消 LLM 选招；LLM 只负责用户意图解析、棋评和聊天，模型失败不阻塞引擎落子。
- 六档改为 Skill Level -20、-12、-4、4、12、20，移除分档深度限制和插件额外失误机制；保留单线程、绑核和搜索时间上限。
- 第六档显示为“全力”，兼容旧“超人类”指令和数字存档；映射尚未经过真人棋力标定。

### 实现细节

- 校验 bestmove 合法性后先保存落子再异步棋评；分析缺失时不借用其他走法评分。
- 删除纯 LLM 选招分支，关闭引擎表示暂停自动落子；旧选招模型、选招超时和候选数配置不再使用。
- 验证跨群 Skill 设置、非首位 bestmove、模型不可用和迟到棋评不影响落子。

## 0.4.1

### 主要功能

- 支持 Linux x86_64 的 WebUI 一步安装：固定版本引擎随插件分发，首次加载离线校验并准备，无需任何手动安装脚本。
- 引擎子进程自行绑核，移除 taskset 系统依赖；MaiBot 和共享 runner 的 CPU 调度保持不变。

### 实现细节

- 内置引擎校验 SHA256 后原子写入 SDK 数据目录，有效缓存直接复用，损坏缓存从内置资源恢复；自定义引擎不被覆盖。
- 分发对应版本源码归档与 GPL 许可证，移除 install_engine.py，Python 依赖继续由宿主按清单安装。
- 增加离线准备、完整性校验、失败清理及无外部命令的 SDK 首次加载测试；Linux CI 不再运行引擎安装脚本。

## 0.4.0

### 主要功能

- 引擎随 MaiBot 插件加载自动启动，随卸载自动退出；日常直接启动 MaiBot，无需独立启动脚本。
- 引擎单线程、固定逻辑 CPU；新增 `engine.cpu`，保持宿主原有调度，不再预留独占物理核心。

### 实现细节

- 收拢 UCI 进程、握手、串行搜索、配置切换与回收，复用进程并在每次搜索前重置棋局状态。
- 插件加载失败主动关闭数据库与引擎；取消或协议错误回收进程，棋局保留并可重试。
- 移除 `run_isolated.py` 及其祖先进程核验；首次引擎安装脚本继续保留。
- 增加真实子进程生命周期测试，Linux CI 改用普通启动验证绑核与六档引擎搜索。

## 0.3.2

### 难度调整

- 简单、标准、困难、挑战的搜索深度上限分别降为 1、2、4、6；已有棋局重启后按原档位采用新上限。
- 标准和困难加入受控失误机制，分别以 30% 和 15% 概率尝试较弱候选，且损失范围逐档缩小；挑战档不主动制造失误。
- 新增第 6 档「超人类」，沿用原挑战仅受时间预算限制的搜索方式，不主动制造失误；名称不代表真人棋力认证。
- 保留入门和简单档的失误机制，以及所有档位的时间上限、单核隔离和关闭 NNUE 的配置。

## 0.3.1

### 主要功能

- 继续降低入门和简单档：浅搜索评估全部合法着法，再按概率提供受控的较弱候选；简单档深度由 3 降至 2。
- 入门档以 85%、简单档以 60% 的概率尝试失误候选；入门允许更大的评分损失。目标池中没有最优招，LLM 无法从中重新选回最优招。

### 实现细节

- 没有合适的较弱候选时正常选招；存在非必败招时，不主动提供引擎已判为强制输棋的招。
- 随机性仅用于候选池，LLM 仍最终拍板；模型失败时继续保留棋局，不随机代走。
- 标准及以上保持原候选策略；所有档位保留单线程、CPU 隔离和搜索时间上限。

## 0.3.0

### 主要功能

- 增加入门、简单、标准、困难、挑战五档引擎难度；新局默认标准。
- 开局可带难度，例如「下棋 开始 黑 简单」；「下棋 难度」查看、「下棋 难度 2」由棋手中途调整。
- 难度随棋局保存，群与群独立；旧存档继续使用原搜索方式，对应挑战档。

### 实现细节

- 通过实际搜索深度上限调节候选质量，LLM 继续最终选招；不把 Skill Level 或 UCI_Elo 当作真人象棋等级分。
- 思考期间拒绝修改难度，调整后下一次搜索生效，棋谱不变；新局默认配置变更不影响已有棋局。
- 所有档位保持单核隔离、单搜索线程及部署者设置的时间上限；查看或调整难度不延长占桌时间。

## 0.2.1

### 修复

- 隔离启动器在设置亲和性前检查启动命令是否可执行，缺少 uv 时给出直接使用虚拟环境 Python 的提示。
- 捕获启动文件消失或解释器缺失等 exec 错误，避免只显示 FileNotFoundError 堆栈；保留虚拟环境 Python 的符号链接路径及原命令参数。

## 0.2.0

### 主要功能

- 引擎提供候选和分析，LLM 最终选招；默认 Fairy-Stockfish 14 largeboard，单搜索线程、3 候选、每步 800ms。
- Linux 启动器为引擎预留整个物理核心，MaiBot 与插件进程排除其超线程；自动读取拓扑，不硬编码 i7 的 CPU 编号。
- bot 落子后自动解说；对局棋手直接聊本局即可，无需聊天指令。聊天不会落子，围观者与无关话题继续普通群聊。

### 实现细节

- 引擎跨群串行运行，取消和退出时回收；规则库再次检查候选及变化，不接受候选外编号。
- 安装器使用固定官方引擎版本及 SHA256；无需下载 NNUE 权重。
- 聊天通过宿主真实入站 Hook 接入，只有成功发送棋局回复才拦截后续普通回复；冷却、超时及棋局快照限制防止频繁或迟到回复。
- 模型或引擎失败保留棋局，不偷偷代选或降级；可明确关闭 engine.enabled 使用旧模式。

## 0.1.1

### 主要功能

- bot 选招新增独立的 `move_timeout`，默认 120 秒；自然语言解析继续使用 `request_timeout`。

### 修复

- 修复合法编号被说明文字或 Markdown 包裹时误判无效的问题，兼容数字字符串。
- 修复超时、接口失败和空响应被统一覆盖成「两次未能返回有效走法」的问题，保留每次失败原因。
- 选招与解析重试会附上具体错误和合法编号范围，减少重复的格式错误。
- 增加不含正文和凭据的诊断日志；拒绝多答案、重复字段和未完成思考，避免误落子。

## 0.1.0

### 主要功能

- 每群一盘，发起者与 LLM 对弈，可选执红或执黑。
- 支持中文棋谱、固定坐标、自然语言解析和歧义确认。
- Pillow 中文棋盘，标记最近双方走法，附可关闭的人设棋评。
- 支持重试、悔棋、认输、管理员结束、空闲清理及重启续局。

### 实现细节

- pyffish 检查合法走法、将军、将死和困毙；选招完全由 LLM 完成。
- SQLite 保存棋谱，按群隔离并去重消息；过期模型回复不会修改新局。
- 自定义三次重复及 120 个半回合无吃子和棋规则，阈值可配置。
- 内置 OFL 中文字体子集，Linux 无需额外安装字体或浏览器。
